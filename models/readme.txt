This folder will contain models based on each module.

This means that whenever a new module is set up or installed a folder will be created and all the related models will be under this folder.

e.g

models/module_name/model.php
